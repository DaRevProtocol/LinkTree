---
import Social from './Social.astro';

export interface Props {
	subtitle: string;
	title: string;
	tagline: string;
}

const { subtitle, title, tagline } = Astro.props;
const socials = await Astro.glob("./../pages/socials/*.md");
---

<div>
    <div class="mx-auto max-w-7xl py-16 px-4 sm:py-10 sm:px-6 lg:px-8  ">
        <div class="text-center">

            <img class="mx-auto h-40  xl:h-56 " src="avatar.png" alt="">
            
            <p class="mt-5 text-4xl font-bold tracking-tight text-gray-100 sm:text-5xl lg:text-6xl">
                {title}
            </p>

            <h2 class="text-lg font-semibold text-indigo-200 mt-5 hidden ">
                {subtitle}
            </h2>

            <p class="mx-auto mt-5 max-w-xl text-xl text-indigo-200">
                {tagline}
            </p>
        
            <Social
                socials={socials}
            />
        </div>
    </div>
</div>
